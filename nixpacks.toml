# Nixpacks deployment configuration for Dokploy
[variables]
BUILD_CACHE_BUSTER = "v3.5.0-NIXPACKS-FORCE-NO-CACHE-AUG5"
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
