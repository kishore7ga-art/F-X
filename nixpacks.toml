# Nixpacks deployment configuration for Dokploy
[variables]
BUILD_CACHE_BUSTER = "v4.1.0-AUTO-OVERLAP-SEP20"
NIXPACKS_NO_CACHE = "1"
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
