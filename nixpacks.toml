# Nixpacks deployment configuration for Dokploy
[variables]
BUILD_CACHE_BUSTER = "v5.1.0-STUDIO-CANVAS-RESTORED-SEP20"
NIXPACKS_NO_CACHE = "1"
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
